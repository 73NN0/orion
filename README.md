# ORION

just some patch to use ioquake as base engine for future game jam

## setup

```sh
./scripts/bootstrap.sh
./compile.sh # should find better name and fix the location
```

inside engine/

```sh
 cmake --build build --parallel 4

```

It's compatible with unix 's patch program

In active development

## TODO:

UI:

- Add more ui primitive
- explore ui framework

MISC:

- try to make a little map and WASD a character
